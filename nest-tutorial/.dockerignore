# Dependency (Quan trọng nhất)
node_modules

# Build output
dist
.next
build

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker files (Không cần thiết bên trong image)
Dockerfile
.dockerignore
docker-compose.yml

# Git
.git
.gitignore

# Environment variables (Bảo mật - không nên cho vào image)
.env
.env.*
!.env.example

# IDE & OS files
.vscode
.DS_Store
Thumbs.db